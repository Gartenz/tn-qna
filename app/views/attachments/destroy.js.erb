var node = ''
<% if is_question?(record) %>
  node = '.question-files'
<% end %>
<% if is_answer?(record) %>
  node = '.answer-<%= record.id %>-files'
<% end %>
$(node).html('<%= render partial: "shared/file", collection: record.files %>')
